import { PlaceholderCard } from '@/components/ui/PlaceholderCard';

export default function UsuariosPage() {
  return (
    <PlaceholderCard 
      title="Usuários" 
      description="Aqui vai ser a listagem (CRUD) dos Usuários." 
    />
  );
}