import { PlaceholderCard } from '@/components/ui/PlaceholderCard';

export default function EquipamentosPage() {
  return (
    <PlaceholderCard 
      title="Equipamentos" 
      description="Aqui vai ser a listagem (CRUD) dos Equipamentos." 
    />
  );
}