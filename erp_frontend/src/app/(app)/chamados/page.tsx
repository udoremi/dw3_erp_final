import { PlaceholderCard } from '@/components/ui/PlaceholderCard';

export default function ChamadosPage() {
  return (
    <PlaceholderCard 
      title="Chamados" 
      description="Aqui vai ser a listagem (CRUD) dos Chamados." 
    />
  );
}